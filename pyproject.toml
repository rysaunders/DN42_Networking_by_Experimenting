[project]
name = "dn42-networking-by-experimenting"
version = "0.1.0"
description = "A hands-on book/site for learning networking through DN42 experiments."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mkdocs-material>=9.5,<10",
]

[tool.uv]
package = false
